Presentations repository

Place presentation source files in the `presentations/` folder. Each presentation should live in its own subfolder (e.g. `presentations/transformers/`) and include an `index.html` file.

This repo is published to GitHub Pages (gh-pages branch). The site root lists presentations and links to each one at `/<presentation-name>/`.
